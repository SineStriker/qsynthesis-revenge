{
    "config": {
        "version": "0.0.1",
        "parent": "QsIntegrate"
    },
    "locales": [
        {
            "key": "${PARENT}",
            "dir": "${FILEPATH}/${CURRENT_KEY}.res/translations"
        }
    ],
    "themes": [
        {
            "key": "${PARENT}",
            "dir": "${FILEPATH}/${CURRENT_KEY}.res/themes",
            "files": {
                "Zhibin - Dark": [
                    "import-dialog-dark.json"
                ],
                "Zhibin - Light": [
                    "import-dialog-light.json"
                ]
            }
        }
    ]
}