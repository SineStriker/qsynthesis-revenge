{
    "config": {
        "version": "0.0.1",
        "parent": "QsIntegrate"
    },
    "locales": [
        {
            "key": "${PARENT}",
            "dir": "${FILEPATH}/${CURRENT_KEY}.res/translations"
        }
    ]
}