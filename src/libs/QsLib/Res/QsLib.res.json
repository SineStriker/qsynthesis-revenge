{
    "config": {
        "version": "0.0.1",
        "parent": "QsLib"
    },
    "locales": [
        {
            "key": "${PARENT}",
            "dir": "${FILEPATH}/translations"
        }
    ]
}