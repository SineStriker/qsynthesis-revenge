#include "../FileParser.h"
#include "QUtauSections.h"

using namespace Utau;

bool FileParser::parseSynthVFile(const QString &filename, SectionNotes &notes) {
    return false;
}

bool FileParser::parseSynthVOldFile(const QString &filename, SectionNotes &notes) {
    return false;
}
