#include "../FileParser.h"
#include "QUtauSections.h"

using namespace Utau;

bool FileParser::parseVocaloidFile(const QString &filename, SectionNotes &notes) {
    return false;
}

bool FileParser::parseVocaloidOldFile(const QString &filename, SectionNotes &notes) {
    return false;
}
