{
    "config": {
        "version": "0.0.1",
        "parent": "MidiPorter"
    },
    "locales": [
        {
            "key": "${PARENT}",
            "dir": "${FILEPATH}/${CURRENT_KEY}.res/translations"
        }
    ]
}