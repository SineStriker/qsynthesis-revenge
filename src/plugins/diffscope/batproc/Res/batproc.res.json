{
    "config": {
        "version": "0.0.1",
        "parent": "batproc"
    },
    "locales": [
        {
            "key": "${PARENT}",
            "dir": "${FILEPATH}/${CURRENT_KEY}.res/translations"
        }
    ]
}