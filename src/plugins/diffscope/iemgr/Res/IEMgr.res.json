{
    "config": {
        "version": "0.0.1",
        "parent": "IEMgr"
    },
    "locales": [
        {
            "key": "${PARENT}",
            "dir": "${FILEPATH}/${CURRENT_KEY}.res/translations"
        }
    ]
}